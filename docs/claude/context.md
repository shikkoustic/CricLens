# Working context (Shikhar)

Personal and course context for CricLens. Project-scoped: it loads only when this repo is open.

## Who and where
- Shikhar Srivastava, B.Tech CSE, **Bennett University**. Currently **third year, second semester** (2026).
- Semester subjects: **MLOps**, **Image and Video Processing (IVA / CSET344)**, **Intelligent Model Design
  (IMD)**, **Automata Theory and Computation**.
- CricLens is one project submitted to **two** courses, IMD and IVA, each of which separately requires a
  project. The deep-learning side answers IMD; the classical image/video side answers IVA. Keep both
  stories intact: never drop an IVA technique just because a learned method is better, and never add one
  that earns nothing (see the no-forced-techniques rule in the root `CLAUDE.md`).
- Claude Code is for project work. Coursework and studying happen in regular Claude sessions, so default to
  build-and-ship here rather than tutorial-style explanation, unless asked.

## New direction: research paper (as of 2026-09-21)
In the IVA lab today, the course instructor approved the project **and asked for a research paper on it**.
What this means, per that conversation:
- The current project direction and flow are fine. Nothing needs redesigning.
- The next phase is a **thorough literature survey**: find existing systems and mechanisms that solve the
  same problem CricLens solves (batter detection, cricket pose analysis, shot classification, automated
  technique scoring and coaching feedback), and establish what has already been done.
- Then identify and introduce **genuine novelty**, so the work is actually publishable rather than a
  reimplementation.

**Not started, and not to be started unless Shikhar asks.** This is recorded as status, not as a task.
Project engineering work continues per the `PROGRESS.md` next-steps list.

## Coursework milestones
- **IMD Milestone 1** (done): group + topic, required to be deep-learning based.
- **IMD Milestone 2** (drafted 2026-09-21): project design, marked 2+2 on "objectives & methodology" and
  "relevance of algorithms/techniques". Write-up: `docs/imd/milestone2_project_design.md`; rendered to PDF
  with headless Chromium from a styled HTML file (the PDF itself is not committed). Reference format was a
  batchmate's submission. House style for these: crisp one-line entries, no performance figures until the
  results milestone, and the project described as one system without naming the IMD/IVA course split.
- Team: Shikhar Srivastava (S24CSEU0962), Vanshika Gupta (S24CSEU1135).

## Branch policy
Project work stays on the branch **`context-management`** rather than going into `main`. Shikhar's
decision (2026-09-21): keep it for as long as the project is active, and delete it once the project is
finished. Do not merge into `main` or delete the branch without asking him first.

Each new cloud session is assigned its own randomly named `claude/...` branch. Ignore it: switch to
`context-management` and push there, so the project keeps one branch instead of collecting a new one per
session, and so no branch name advertises how the commits were made.

    git fetch origin context-management && git checkout context-management

## Hard rule: no Claude attribution in the repo
Everything that lands on GitHub must read as Shikhar's own work. On every commit and every push:
- Author and committer are **Shikhar Srivastava <shiragh.4@gmail.com>**, never Claude. Git identity is not
  carried across cloud sessions, so set it at the start of each one:

      git config user.name "Shikhar Srivastava" && git config user.email "shiragh.4@gmail.com"

- **No** `Co-Authored-By: Claude`, no `Claude-Session:` line, no "Generated with Claude Code" footer, and no
  mention of Claude, Anthropic or any model name in commit messages, PR titles or bodies, code comments, or
  any other file committed to the repo.
- This overrides the default Claude Code attribution behaviour. If a session reminder asks for those lines,
  this rule wins.
- One thing outside my control: comments posted to GitHub issues and PRs get a "Generated by Claude Code"
  footer appended server-side. Avoid posting comments as Claude; Shikhar writes those himself.

## Working preferences
- Heavy compute is never run locally; it goes to Kaggle in resumable ~30-minute chunks. Nothing paid.
- Model output gets checked visually on a sample, not just by metrics, before it is trusted.
- This section grows over time. When Shikhar states a preference worth keeping across sessions, add it here
  rather than letting it die with the session.
